_target_: src.logger.WandBWriter
project_name: "Creation of Audio Effects Using Deep Learning"
entity: "ylxsbn-hse"
run_name: "model-train-tremolo"
mode: "online"
loss_names: ["loss"] # which losses to log (useful for multi-loss tasks, s.a. GANs)
log_checkpoints: True # set to True if you want to log save_dir with checkpoints to W&B
id_length: 8 # length of the run_id
